services:
  ppl_api:
    container_name: ppl_api
    volumes:
      - ./prisma:/app/prisma
    ports:
      - "8082:8082"
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    command: npm run start:prod

volumes:
  prisma: