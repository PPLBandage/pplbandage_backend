services:
  ppl_api:
    container_name: ppl_api
    volumes:
      - ./prisma:/app/prisma
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - nginx
    restart: unless-stopped
    command: npm run start

volumes:
  prisma:

networks:
  nginx:
    external: true
    name: nginx
